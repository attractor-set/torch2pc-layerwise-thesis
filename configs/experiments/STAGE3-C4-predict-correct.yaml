stage3:
  candidate_id: predict_correct_initialization
  track: approximation
  changes_algorithm: true
  implementation_status: planned
  predict_correct:
    initializer: layerwise_ema_residual
    ema_beta: 0.9
    reset_policy: epoch
    exact_correction_steps: null
    candidate_correction_steps: [1, 2, 3, 5]
    fallback_to_strict: true
    fallback_on:
      - non_finite_residual
      - residual_increase
