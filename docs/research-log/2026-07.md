# Журнал исследования: июль 2026

[English version](2026-07_EN.md)

## 2026-07-10

- пакет диссертации преобразован в репозиторий, готовый к публикации на GitHub;
- разделены код, конфигурации, анализ, диссертация, статья и литература;
- исходный ноутбук сохранён как точка проверки миграции;
- подготовлена контролируемая среда Ubuntu/ROCm для RX 7700 XT;
- русский язык назначен основным;
- английские материалы переведены на схему `_EN`.

## 2026-07-12—13

- Torch2PC закреплён на коммите
  `00c6c50ee3540537bbb56ab2b6567b541f42b093`;
- собран и проверен контролируемый образ ROCm для RX 7700 XT;
- C0 и C1 прошли заранее заданные пороги CPU/GPU;
- после исправления перечисления сетки завершены 96 из 96 пилотных ячеек;
- тестовая выборка в пилотном исследовании не вычислялась, неудачных ячеек не
  зарегистрировано;
- выбраны FixedPred `eta=0.1`, `n=10` и Strict `eta=0.05`, `n=20`;
- расчёт требуемой точности указал 10 парных случайных начальных значений
  модели для итогового выполнения; это рекомендация, а не гарантия мощности.

## Следующее действие

Сохранить компактные пилотные наблюдения и [происхождение артефактов](../glossary.md#term-provenance), затем
пересобрать и зафиксировать среду с выбранными параметрами, повторить C0/C1 и
создать `pilot-freeze`.

## 2026-07-13 — постпилотная фиксация итогового выполнения

До доступа к итоговой тестовой оценке принято ADR-005. Число итоговых пар
сохранено равным 10 в соответствии с рекомендательным результатом пилотного
исследования. Добавлены неизменяемый план 80 итоговых ячеек,
детерминированное уравновешивание методов, безопасное возобновление, отдельный
снимок пилотного реестра, сводный хэш конфигураций и телеметрия GPU.
Гиперпараметры, основная метрика, граница эквивалентности и основные контрасты
не изменялись.

## 2026-07-13 — завершение этапов 1 и 2

- этап 1 завершён: 80 из 80 подтверждающих ячеек, 0 неудачных;
- для этапа 2 закреплён коммит Torch2PC
  `b20d9142e4bdbf57b3ec8bf9f9c4472372ec8db4`;
- проверки эквивалентности CPU float64 и GPU float32 пройдены;
- этап 2 завершён: 80 из 80 подтверждающих ячеек, 0 неудачных;
- парные тестовые значения точности и макро-F1 этапов 1 и 2 совпали;
- Exact приблизился к времени BP; FixedPred и Strict существенно ускорились;
- сформированы отчёты этапа 2, снимок завершения, манифест результатов и
  межверсионный анализ;
- добавлен аудит изменений Torch2PC и границ подтверждённой эквивалентности.

## 2026-07-13 — сопровождение после эксперимента

Исправляются только инструменты и происхождение публикационных артефактов:
стабильная запись CSV с переводами строк LF, правильная привязка манифеста
этапа 2 к файлу фиксации среды этапа 2 и [запуск](../glossary.md#term-run) генератора плана как модуля.
Файл фиксации выполнения и результаты 80 запусков сохраняются.

## 2026-07-13 — сопровождение после эксперимента завершено

- контрольные суммы снимков этапов 1 и 2 проверены по фактическим файлам;
- манифест результатов этапа 2 привязан к файлу фиксации среды этапа 2 и больше
  не включает сам себя;
- генератор плана выполнения этапа 2 запускается как модуль Python;
- Ruff и набор регрессионных тестов проходят; зарегистрировано 63 успешных
  теста;
- следующий публикационный шаг — теги состояний выполнения и результатов и
  отдельный набор воспроизведения с исходными артефактами запусков.

## 2026-07-13 — проектирование расширенного этапа 3

- этапы 1 и 2 закреплены как неизменяемая [базовая линия](../glossary.md#term-baseline);
- выбраны основные оси: локальность, точная организация VJP, адаптивная
  остановка и периодическое обновление VJP;
- фиксированная случайная обратная связь оставлена условным разведочным
  направлением;
- добавлены протокол этапа 3, ADR, контракт проектирования, схема локальности и
  семейство моделей для масштабирования;
- [выполнение](../glossary.md#term-execution) остаётся заблокированным до реализации кандидатов, прохождения
  проверок и отдельной фиксации.

## 2026-07-16 — предварительная регистрация B1/B2

- зафиксированы отдельные контракты B1 `isolated_layer_vjp` и B2
  `composite_vjp`;
- B1 реализуется первым, B2 остаётся закрытым до зафиксированного `EQ-B1`;
- прямое сравнение B1/B2 и нулевой предел опасных допусков зарегистрированы
  заранее;
- основное измерение времени и структурные счётчики разделены;
- B1/B2 явно отделены от оценивателя, офлайн-политики, гистерезиса и
  `QWake-PC`;
- после `EX-IF0` предусмотрены `A11-OFF0`, `A11-OFF1`, предварительная
  регистрация предиктора и теневое управление;
- реализация, выполнение ROCm и
  [доказательные материалы](../glossary.md#term-evidence) отсутствуют.

## 2026-07-24 — полный рефакторинг `QWake` до нового образа

- [кандидат](../glossary.md#term-candidate) одноразового разрешения `QW-4B-F-v1` выведен из обращения до
  [выполнения](../glossary.md#term-execution);
- его предварительная проверка, квитанция, разрешение и журналы сохранены во
  внешнем каталоге аудита;
- зафиксировано отсутствие инженерных
  [доказательных материалов](../glossary.md#term-evidence) и научного запуска;
- отклонено точечное исправление терминологии как недостаточное;
- активные план, дорожная карта, граница будущей политики, глоссарий и статус
  перестроены вокруг `R/M/Γ/C` и семейства `LOCAL_COMPUTE`;
- старые активные обозначения `QW-6`–`QW-10` заменены ролями `C1/C2/C3/R`;
- после слияния обязателен новый неизменяемый образ, новая предварительная
  проверка и новое одноразовое разрешение.

### Проверка миграции документационных тестов `QW-4B-DOC-R1`

Строгие проверки структуры языка, глоссария, локальных ссылок и обе сборки
документации прошли. Полный набор тестов обнаружил шесть исторических проверок,
которые всё ещё считали последовательность `QW-0…QW-10` активной и требовали
старые маркеры в полностью переработанном плане.

Вместо возврата двух конкурирующих дорожных карт проверки разделены на два слоя:
сохранность исторических границ `QW-2/QW-3/QW-4` и актуальная последовательность
`QW-4B-DOC-R1 → QW-4B-F-v2 → QW-4B-E-v2 → QW-LC → QW-5 → C1/C2/C3/R`.
Одновременно в активные планы возвращены точные пары `P0/P1/P2`, закрывающие
маркеры выполнения и строго офлайн-контракт `C2`.

### `QW-4B-F-v2`: новая фиксация допуска среды без выполнения

- новый образ собран из коммита слияния `e413bb1e13cee42f702512e499f994e90df21e45` и связан с Torch2PC `b20d9142e4bdbf57b3ec8bf9f9c4472372ec8db4`;
- повторно пройдены 17 статических и модульных проверок, включая строгую двуязычную документацию;
- живая предварительная проверка связала CPU/float64 и ROCm/float32 с образом `sha256:bd91fab26df5f91a3aba90b8cad38badccab3a1a7bfb20efe4126a88a13236c4`;
- выпущено и повторно проверено разрешение на шесть ячеек `P0/P1/P2`, один синтетический пакет, начальное случайное значение 0 и одну попытку;
- восемь внешних файлов сохранены побайтно, а два реестра SHA-256 связывают исходный и полный зафиксированный пакет;
- исполняющая команда не запускалась, каталог результатов отсутствует, инженерные доказательные материалы и научная кампания закрыты;
- следующий отдельный срез — `QW-4B-E-v2`; `QW-LC0` остаётся закрытым.

### `QW-4B-E-v2`: одноразовое выполнение и восстановительное решение

- одна разрешённая [попытка](../glossary.md#term-attempt) выполнена из
  изолированного снимка исходного кода для коммита
  `e413bb1e13cee42f702512e499f994e90df21e45`; вычисление повторно не запускалось;
- шесть ячеек `CPU/ROCm × P0/P1/P2` завершились успешно, выход содержит ровно
  шесть файлов и связан с отчётом
  `sha256:54dba01d47814dc00fa53bd69c00865bd1c47754c017c7482c895162d3a86b82`;
- исходная квитанция завершения сохраняет ошибку обёртки после успешного
  вычисления; два неудачных восстановительных аудита также сохранены;
- восстановление v3 подтвердило равенство `JSON` и загруженных моделей
  разрешения, обе линии, вложенность наблюдений, изоляцию эталонной проверки и
  отсутствие эффектов отключённых возможностей;
- материализованы точный выход, 21-файловый пакет аудита и двухфайловая
  внешняя печать; всего 29 файлов;
- разрешение использовано, повтор запрещён, результат остаётся только
  инженерными [доказательными материалами](../glossary.md#term-evidence), а
  публикация и `QW-LC0` закрыты до фиксации в репозитории.


### Переход после слияния `QW-4B-E-v2` к `QW-LC0`

- PR #110 слит коммитом слияния `4f23b752a40ae05de9fc7ee49c9962c44083b71d` с сохранением
  коммита печати репозитория `26bc0ef635e13dba719d3356fe17382f0037d1df` как второго родителя;
- точные выход, пакет аудита и внешняя печать повторно прошли проверки
  SHA-256, семантики и целостности на `main`;
- доказательные материалы репозитория признаны запечатанными;
- `QW-LC0` открыт только для отдельной фиксации семантики и области;
- реализация и выполнение `LOCAL_COMPUTE`, научный образ, C1/C2/C3/R,
  тестовая выборка и публикация остаются закрытыми.


### Фиксация семантики и области `QW-LC0`

- запечатан контракт `stage3b-qwake-lc0-semantics-scope-v1` с разделением `R/M/Γ/C`;
- `LOCAL_COMPUTE` ограничен видами `LOCAL_SWEEP` и `ANALYTIC_COMPLETION`;
- первый кандидат ограничен `FixedPred`, `eta=1`, `lenet_classic` и
  `stage2_baseline`;
- точный ответ отложен до `QW-LC1`, ресурсная модель и стоимость — до
  `QW-LC2`;
- реализация, выполнение, научная кампания, тестовая выборка и публикация
  остаются закрытыми.

### Фиксация состояния репозитория `QW-LC0`

- контракт `stage3b-qwake-lc0-semantics-scope-v1` слит в `main`
  `8429f54257685a879b0a44499d5fa81eab7310ea` с сохранением коммита контракта
  `715308451ac3e696d4c2209276d36853f6799d6f` вторым родителем;
- 22-файловое дерево, контракт и реестр повторно проверены без изменений;
- материализована двухфайловая квитанция состояния репозитория;
- переход к `QW-LC1`, реализация, выполнение, сбор признаков, создание
  эталонных меток, тестовая выборка и публикация остаются закрытыми до слияния
  квитанции и отдельной проверки после слияния.

### Переход к `QW-LC1`

- фиксация состояния репозитория `QW-LC0` слита в `main` `0fbd54be337665e06ad63b6d9c7f8ca978ab75ee` и
  повторно проверен;
- материализована двухфайловая квитанция перехода
  `stage3b-qwake-lc1-transition-v1`;
- область следующего среза ограничена схемой `R(a,s)`, обязательными
  наблюдаемыми полями и оператором `~R`;
- `QW-LC1`, `Γ`, стоимость, реализация, выполнение, сбор признаков, эталонные
  метки, тестовая выборка и публикация остаются закрытыми до слияния перехода.
### Схема требуемого результата `QW-LC1`

- переход `QW-LC1` слит в `main` `c3533fcb63ffc869faddbaa99645c9099d16d1cc` и повторно проверен;
- запечатан контракт `stage3b-qwake-lc1-required-response-schema-v1`;
- `R(a,s)` ограничен именованными градиентами параметров, конечными представлениями слоёв и скалярной функцией потерь;
- каноническая сериализация разделяет манифест `JSON` и исходные файлы полезных данных тензоров;
- обязательные наблюдаемые поля фиксируют структуру, признаки конечности и `SHA-256`;
- `~R` использует точную структурную проверку и отдельные численные проверки каждой записи с учётом нулевых норм, не предполагая транзитивность;
- профили CPU/float64 и ROCm/float32 зафиксированы, ROCm остаётся решенческим;
- сопоставление состояния, генераторов и резервного пути отложено до `QW-LC3`, а `Γ`, `Φ`, `C` и `~C` — до `QW-LC2`;
- реализация, выполнение, сбор признаков, эталонные метки, тестовая выборка и публикация остаются закрытыми.
### Фиксация состояния репозитория `QW-LC1`

- схема `stage3b-qwake-lc1-required-response-schema-v1` слита в `main` `59e3143ba105a5b298e2cd551b221b8f6dae96f7` с сохранением коммита `de2b5a37583b22946073390caa244bee35dd793b` вторым родителем;
- точный 22-файловый состав, дерево схемы, контракт и реестр повторно проверены без изменений;
- материализована двухфайловая квитанция состояния репозитория;
- `QW-LC1` остаётся незавершённым, а переход к `QW-LC2`, [ресурсная траектория](../glossary.md#term-resource-trajectory), стоимость, реализация, выполнение и публикация закрыты до слияния и отдельной проверки квитанции.

### Переход к `QW-LC2`

- фиксация состояния репозитория `QW-LC1` слита в `main` `9d073bc3c90eeda53ca03d0f7762b65da8749269` и
  независимо проверена;
- `QW-LC1` завершён, а переход к `QW-LC2` разрешён;
- материализована двухфайловая квитанция
  `stage3b-qwake-lc2-transition-v1`;
- область следующего контракта ограничена схемой `Γ(a,s)`, отображением `Φ`
  и оператором `~C`;
- поля, единицы, стоимость, сопоставленная теневая проверка, реализация,
  выполнение, тестовая выборка и публикация остаются закрытыми до слияния
  перехода.

### Контракт ресурсов и стоимости `QW-LC2`

- переход `QW-LC2` слит в `main` `858403cbb2423ad3427ab7a042266880ca34c0b7` и независимо проверен;
- материализован контракт `stage3b-qwake-lc2-resource-cost-contract-v1`;
- зафиксированы упорядоченная `Γ`, отображение `Φ` без двойного учёта,
  11-полевой `C`, инженерный теневой и будущий сквозной профили;
- `~C` определён покомпонентно с допусками CPU/ROCm без предположения
  транзитивности;
- правило Парето применяется только после допуска по `~R`, неоднозначность
  разрешается зарегистрированным детерминированным правилом, а неполный вектор
  ведёт к `LOCAL_SWEEP`;
- `QW-LC3`, реализация, выполнение, тестовая выборка и публикация остаются
  закрытыми.

### Фиксация состояния репозитория `QW-LC2`

- PR №118 слит в `main` `8f24229bcf19736086fe6f0340bda26dd533936a` и независимо проверен;
- сохранены точные родители и дерево `3f1682765089b0819dcaaf9bb449c4c1bd155142`;
- материализована квитанция `stage3b-qwake-lc2-repository-freeze-v1`;
- `QW-LC2` незавершён до её слияния; `QW-LC3`, реализация, выполнение,
  тестовая выборка и публикация закрыты.

### Переход к `QW-LC3`

- фиксация состояния репозитория `QW-LC2` слита через PR №119 в `main`
  `4f7c533047214398e7ec4dde9d58b5fc06964b90` и независимо проверена;
- коммит фиксации `3f4310a05de5b7cd3db0cdb5c8f7cf4bbcb09150` сохранён, дерево не
  изменено, квитанция и контракт ресурсов соответствуют ожидаемым SHA-256;
- `QW-LC2` завершён, переход к `QW-LC3` разрешён;
- материализована двухфайловая квитанция
  `stage3b-qwake-lc3-transition-v1`;
- область будущего контракта ограничена сопоставленной теневой проверкой,
  общим состоянием, восстановлением ГПСЧ, полным точным резервным суффиксом и
  агрегацией повторов;
- `QW-LC3`, реализация, выполнение, сбор признаков, эталонные метки, тестовая
  выборка и публикация остаются закрытыми до слияния перехода.

### Контракт сопоставленной теневой проверки `QW-LC3`

- переход `QW-LC3` слит через PR №120 в `main`
  `a7e0c4ec1978042d68abc7437e3005e4295e75ff` и независимо проверен;
- материализован контракт
  `stage3b-qwake-lc3-matched-shadow-validation-contract-v1`;
- зафиксированы канонический `opaque_state_ref`, одноразовые ответвления общего
  снимка и точное восстановление полного реестра ГПСЧ;
- определены двенадцать сопоставленных повторов с равным числом двух порядков
  рук и запретом исключения повторов;
- два принудительных зонда проверяют полный точный резервный суффикс до первого
  и после последнего повтора;
- стоимость агрегируется покомпонентно по парным разностям без скаляризации и
  без заявления статистической значимости;
- реализация, выполнение, сбор признаков, эталонные метки, тестовая выборка и
  публикация остаются закрытыми.

### Фиксация состояния репозитория `QW-LC3`

- контракт `stage3b-qwake-lc3-matched-shadow-validation-contract-v1` слит через PR №121 в `main` `71e73f56408c720334b8fa03e7133762c8bbcc43` и независимо проверен;
- сохранены точные родители и дерево `fb3f1cd4a4d3b4261db1179badcc1ccacddfe936`;
- материализована квитанция `stage3b-qwake-lc3-repository-freeze-v1`;
- `QW-LC3` незавершён до её слияния; `QW-LC4-I`, реализация, выполнение, сбор признаков, эталонные метки, тестовая выборка и публикация закрыты.

### Ограниченная реализация `QW-LC4-I`

- фиксация состояния через PR №122 слита в `main`
  `7c6cbb6ba4941cf78b2bfec3e6e8955c2830a58b` и независимо проверена;
- `QW-LC3` завершён, подготовка ограниченной реализации разрешена;
- модуль `stage3b_qwake_lc4_bounded.py` материализует завершение волны при
  `eta=1` и полный точный суффикс из одноразовых ответвлений общего состояния;
- реализованы канонический контроль состояния и ГПСЧ, `~R`, отображение
  `Γ -> C`, резервные зонды, расписание двенадцати пар и покомпонентная
  агрегация;
- доступен только синтетический модульный тест; выполнение в рабочей среде,
  научные данные, сбор признаков, эталонные метки, активация политики и
  публикация закрыты до `QW-LC4-F`.

### Авторинг фиксации рабочей среды `QW-LC4-F`

- `QW-LC4-I` слит через PR №123 и независимо проверен на `main` `c9f3dadcd5330887584b8bf71d906c667dacf076`.
- Заморожен запрос `stage3b-qwake-lc4-f-runtime-freeze-request-v1`: две полосы, индексы кандидата `0..6`, 12 повторов на ячейку, 168 сопоставленных пар и 28 резервных зондов.
- Добавлены адаптер состояния рабочей среды, запрещающий все возможности `preflight`, схема разрешения одной попытки и скрипт фиксации без исполнителя.
- Хэш образа намеренно не задан до коммита авторинга; после коммита образ должен быть построен именно из него.
- Выполнение в рабочей среде, инженерное свидетельство, научное выполнение, [набор данных](../glossary.md#term-dataset), публикация и активация политики остаются закрытыми.
### Фиксация рабочей среды `QW-LC4-F`

См. [ADR-063](../decisions/ADR-063-stage3b-qwake-lc4-f-runtime-freeze.md).

- из точного коммита авторинга зафиксирован проверенный образ;
- проверки CPU/ROCm и 22 статические проверки завершились успешно;
- одноразовое разрешение охватывает 168 сопоставленных ячеек;
- материализован десятифайловый пакет
  `stage3b-qwake-lc4-f-runtime-freeze-v1`;
- `image-build.log` явно фиксирует реконструированное происхождение исходного
  образа; отдельная повторная сборка не используется для идентичности;
- исполнение не производилось, `QW-LC4-F` незавершён до слияния, а
  `QW-LC4-E` и научные возможности закрыты.
### Авторинг допуска `QW-LC4-E`

См. [ADR-064](../decisions/ADR-064-stage3b-qwake-lc4-e-execution-admission-authoring.md).

- ветка открыта от проверенного коммита слияния `453bb4eb...`;
- схема повторно проверяет точный пакет `QW-LC4-F`;
- допуск требует отдельное подтверждение оператора и отсутствие каталога результатов;
- запись допуска, файл владения, исполнитель и инженерные материалы отсутствуют;
- фактическое выполнение остаётся закрытым.
### Фиксация допуска `QW-LC4-E`

См. [ADR-065](../decisions/ADR-065-stage3b-qwake-lc4-e-execution-admission-freeze.md).

- авторинг допуска слит через PR №125 в `main` `bce821dff0729629db0ccb306d8f3fd1dd9a2e13` и независимо проверен;
- материализован пятифайловый пакет допуска;
- запись связана с точным коммитом контура управления и разрешает одну попытку;
- отсутствие каталога результатов и файла владения проверено;
- разрешение выполнения на ветке, исполнитель, доказательные материалы, тестовая выборка и публикация закрыты.
### Проектирование владения и исполняющей обёртки `QW-LC4-E`

См. [ADR-066](../decisions/ADR-066-stage3b-qwake-lc4-e-execution-lease-wrapper-authoring.md).

- фиксация допуска слита в `main` `12b7d24153a681f731a43e8497275016ad4e1656` и независимо проверена;
- реализована чистая предварительная схема одноразового файла владения;
- реализован контракт исполняющей обёртки для 168 ячеек и 28 резервных проверок;
- будущий захват должен быть атомарным, исключительным и без повтора;
- программы записи владения и результатов, исполнитель рабочей среды и каталог результатов отсутствуют;
- разрешение не потреблено и выполнение не начато.

### Атомарная реализация владения и исполняющей обёртки `QW-LC4-E`

См. [ADR-067](../decisions/ADR-067-stage3b-qwake-lc4-e-execution-lease-wrapper-implementation.md).

- PR №127 слит в `main` `e0455dc77b49f5b220231509fe6062d275b6ee9b` и независимо проверен;
- отдельный модуль реализует исключительный захват владения без замены;
- после захвата выполняется обязательная повторная проверка отсутствия результата;
- вычислительный модуль изолирован временным каталогом, а полный результат продвигается атомарно;
- файл владения сохраняется после ошибки, повтор запрещён, неполный каталог удаляется;
- синтетическая программа проверки и модульные тесты не создают эффекты в репозитории;
- фактический захват, выполнение, свидетельство, тестовые данные и публикация закрыты.
### Авторинг фиксации выполнения `QW-LC4-E`

См. [ADR-068](../decisions/ADR-068-stage3b-qwake-lc4-e-execution-freeze-authoring.md).

- PR №128 слит в `main` `24966cd2a0380e46ab1924ff4ab8987f17e1fe9e`
  и независимо проверен;
- точный головной коммит `15588616c24d523f1c983fc205aeaae32a33958e`, два родителя,
  16-файловое дерево и CI 2/2 подтверждены;
- материализован чистый запрос фиксации выполнения с SHA-256 `sha256:9b28943043082efe96fb313f94875ef18c7f8e7361d8c0eb1b8c140e82a1e312`;
- запрос требует один процесс для захвата и выполнения, запрет повтора, атомарное
  продвижение и каноническую квитанцию вычислительного модуля;
- конкретный вычислительный модуль и одноразовая точка входа отсутствуют, поэтому фиксация
  выполнения и владение не материализованы;
- следующий срез — реализация конкретного вычислительного модуля рабочей среды без запуска.

### `QW-LC4-E`: реализация `bounded runtime backend`

См. [ADR-069](../decisions/ADR-069-stage3b-qwake-lc4-e-runtime-backend-implementation.md).

Открыта отдельная ветка от `merge commit` `49d691d497f4f719e82b271e9b9d441f9e4dfa63`.
Материализованы `concrete backend` и `one-shot entrypoint` без файла владения и без
рабочего запуска. CPU-контроль всех семи кандидатов подтвердил прохождение
`~R`; максимальный `roundoff` уже завершённой верхней волны находится примерно в
диапазоне `1.4e-17..5.6e-17` и обрабатывается чистой ограниченной
канонизацией. Контроль двенадцати повторов показал возможный отрицательный
`order-effect gate`, поэтому он сохранён как результат, а не превращён в потерю
одноразового `evidence`. `Immutable image` и `execution-freeze-v1` отсутствуют;
выполнение остаётся закрытым.

### Материализация фиксации выполнения `QW-LC4-E`

См. [ADR-070](../decisions/ADR-070-stage3b-qwake-lc4-e-execution-freeze-materialization.md).

- PR №130 слит в `main` `67a084c0b970ad79ad0692442f660085a73b080a` и независимо проверен;
- из этого коммита построен неизменяемый образ `torch2pc-layerwise-thesis:0.1.0-qw-lc4-e-freeze-67a084c0b970` с идентификатором `sha256:7da92b8f77f6dc37d42db832c5613ef6149dc488adc5b66465faa33e48ca021d`;
- девятифайловый пакет `execution-freeze-v1` связывает образ, вычислительный модуль, точку входа, допуск и авторизацию;
- исходный `image-build.log` сохраняется байт-в-байт и точечно классифицируется в `.gitattributes` как двоичное запечатанное свидетельство;
- внутренняя запись разрешает будущую одноразовую точку входа, но веточный допуск выполнения остаётся закрытым;
- файл владения, каталог результата, инженерные материалы, научное выполнение, тестовая выборка и публикация отсутствуют.

### Разрешение одноразового инженерного вызова `QW-LC4-E`

- PR №131 проверен после слияния в `375db196b615f7024cd5f715de9c9be7b526a9f7`;
- материализован четырёхфайловый пакет разрешения одного будущего инженерного вызова;
- семантический SHA-256: `sha256:0a60dacc1bfd5073cf52d76f2ec33ae54f00899aad9877d44607199659fda75a`;
- файл владения, результат, выполнение, научные данные и публикация остаются закрытыми.

### Авторинг хостовой обёртки вызова `QW-LC4-E`

См. [ADR-072](../decisions/ADR-072-stage3b-qwake-lc4-e-one-shot-invocation-wrapper-authoring.md).

- PR №132 слит в `main` `8337d9ad0ac21a69a577ab74a73d05d69f8fa7a1` и независимо проверен;
- создан чистый канонический контракт будущей хостовой обёртки;
- разрешены только зафиксированные пакеты и Torch2PC для чтения и каталог результатов для чтения и записи;
- зафиксированы `/dev/kfd`, `/dev/dri`, пользователь, группы, ресурсные входы, точный шаблон команды и временная файловая система `/tmp`;
- исходное дерево, набор данных, сеть, привилегированный режим и лишние возможности запрещены;
- хостовый исполнитель, проверка образа, команда вызова, файл владения и выполнение отсутствуют.

### Реализация хостовой обёртки вызова `QW-LC4-E`

См. [ADR-073](../decisions/ADR-073-stage3b-qwake-lc4-e-one-shot-invocation-wrapper-implementation.md).

- PR №133 слит в `main` `7cc17c6b36cb5115e63a2b64e4bff90a525b2465` и независимо проверен;
- реализована проверка точного локального образа через `docker image inspect` без загрузки образа;
- текущая идентичность сравнивается с замороженной записью по тегу, хэшу, слоям, меткам, переменной исходного коммита, точке входа и рабочему каталогу;
- реализована каноническая проверка 13 входов ресурсов и детерминированная материализация будущего `argv` только в памяти;
- хостовый исполнитель, файл владения, результат, [доказательные материалы](../glossary.md#term-evidence), научное выполнение, тестовые данные и публикация отсутствуют.

### Авторинг одноразового хостового исполнителя `QW-LC4-E`

См. [ADR-074](../decisions/ADR-074-stage3b-qwake-lc4-e-one-shot-host-runtime-invoker-authoring.md).

- PR №134 слит в `main` `be6486a9e3670343132f2c863a5a0cd5969ee9f6` и независимо проверен;
- зафиксирован чистый контракт единственного будущего запуска контейнера;
- последовательность разделяет хостовую повторную проверку и контейнерный атомарный захват файла владения;
- потребление разрешения начинается только при успешном захвате файла владения, а повторная проверка предшествует вычислению;
- тайм-аут, сигналы, ограниченный вывод и терминальный запрет автоматического повтора определены до реализации;
- хостовый исполнитель, файл владения и [выполнение](../glossary.md#term-execution) отсутствуют.

### Реализация одноразового хостового исполнителя `QW-LC4-E`

См. [ADR-075](../decisions/ADR-075-stage3b-qwake-lc4-e-one-shot-host-runtime-invoker-implementation.md).

- PR №135 слит в `main` `7f1655346bca77834d73a660c9857f1ff23b826c` и независимо проверен;
- реализованы двойная проверка образа и канонического `argv`, один `Popen` без оболочки и фиксированная хостовая среда;
- дочерний процесс получает отдельную группу, пересылку `SIGINT`/`SIGTERM`, терминальный тайм-аут и ограниченный вывод;
- хост не записывает файл владения и не сохраняет команду или журналы;
- проверяющая программа и тесты не вызывают рабочую среду Docker, поэтому выполнение и результаты отсутствуют.

### Фиксация состояния репозитория одноразового хостового исполнителя `QW-LC4-E`

См. [ADR-076](../decisions/ADR-076-stage3b-qwake-lc4-e-one-shot-host-runtime-invoker-repository-freeze.md).

- PR №136 слит в `main` `da51c8d858c541372525125640db99062041fc20` в `2026-07-29T15:27:45Z`;
- головной коммит `181abda36465d3a91db5970e684938266200a798`, оба родителя и 16-файловое дерево записаны в квитанции;
- герметичная коррекция теста сохранена отдельным вторым коммитом без переписывания исходной реализации;
- 2/2 CI-проверки, 139 целевых и 1186 полных тестов прошли, предупреждений полного набора — 14;
- материализован двухфайловый пакет `repository-freeze`;
- проверка образа, `docker run`, файл владения, потребление разрешения, выполнение и результат отсутствуют.

### 29 июля — авторинг допуска одноразового инженерного вызова `QW-LC4-E`

- после проверки слияния PR №137 открыта отдельная ветка `invocation` от `3454d12d3cc16c9c50977e2a598e2bc1a8768441`;
- добавлен ADR-077 и канонический пакет `admission`;
- `verifier` повторно связывает `repository freeze`, `authorization` и `host invoker` без эффектов рабочей среды;
- `PREEXECUTION_IDENTITY_VERIFIED=false`, `DOCKER_RUN_PERFORMED=false`, `LOCAL_COMPUTE_EXECUTION_OPEN=false`.
- `decision marker`: `ADR-077-stage3b-qwake-lc4-e-one-shot-engineering-invocation-admission`.

### 29 июля — авторинг записи операции одноразового инженерного вызова `QW-LC4-E`

- после проверки слияния PR №138 открыта отдельная ветка `invocation-operation` от `28be77706bc86abaf34f86e9bdcbdcb9cc2810a8`;
- добавлен ADR-078 и канонический пакет `operation`;
- запись фиксирует точные динамические входы и проверки будущего запуска без эффектов рабочей среды;
- `PREEXECUTION_IDENTITY_VERIFIED=false`, `INVOCATION_COMMAND_MATERIALIZED=false`, `DOCKER_RUN_PERFORMED=false`;
- `decision marker`: `ADR-078-stage3b-qwake-lc4-e-one-shot-engineering-invocation-operation`.

### 29 июля — авторинг авторизации выполнения одноразового инженерного вызова `QW-LC4-E`

- после проверки слияния PR №139 открыта отдельная ветка `invocation-execution` от `b0f6729e8fd1cb1aa172eef488dc56e36b335173`;
- добавлен ADR-079 и канонический пакет `execution-authorization`;
- запись связывает слитую операцию, прежнюю авторизацию, образ, Torch2PC и хостовый исполнитель;
- материализован контракт будущей проверки перед выполнением в рамках одного процесса без эффектов рабочей среды;
- `PREEXECUTION_IDENTITY_VERIFIED=false`, `INVOCATION_COMMAND_MATERIALIZED=false`, `DOCKER_RUN_PERFORMED=false`;
- `decision marker`: `ADR-079-stage3b-qwake-lc4-e-one-shot-engineering-invocation-execution-authorization`.

### 29 июля — контракт проверки перед одноразовым инженерным вызовом `QW-LC4-E`

- после проверки слияния PR №140 открыта отдельная ветка `preexecution-verification` от `49c4b97e93b47cefbf35576736927ece02c9402b`;
- добавлен ADR-080 и канонический пакет `preexecution-verification`;
- контракт связывает слитую авторизацию с точным хостовым исполнителем и запрещает отдельную динамическую проверку вне будущего вызывающего процесса;
- проверяющая программа подтверждает только статические пакеты и реализацию без вызова Docker;
- `PREEXECUTION_IDENTITY_VERIFIED=false`, `INVOCATION_COMMAND_MATERIALIZED=false`, `DOCKER_RUN_PERFORMED=false`;
- `decision marker`: `ADR-080-stage3b-qwake-lc4-e-one-shot-engineering-invocation-preexecution-verification`.

### 29 июля — авторинг ограниченной операции одноразового инженерного вызова `QW-LC4-E`

- после проверки слияния PR №141 открыта отдельная ветка `runtime-operation` от `494e6a0b2f10c26b49c90fbb84c23565699a4064`;
- добавлен ADR-081 и канонический пакет `runtime-operation`;
- точка входа требует явное разрешение, точное подтверждение, время после слияния и полный набор ресурсов хоста;
- динамические проверки и единственное создание дочернего процесса делегируются неизменному `invoke_one_shot_host_runtime` без прямых вызовов Docker в новом модуле;
- `PREEXECUTION_IDENTITY_VERIFIED=false`, `INVOCATION_COMMAND_MATERIALIZED=false`, `DOCKER_RUN_PERFORMED=false`;
- `decision marker`: `ADR-081-stage3b-qwake-lc4-e-one-shot-engineering-invocation-runtime-operation`.

### 29 июля — авторинг восстановления идентичности операции рабочей среды `QW-LC4-E`

- после проверки PR №142 после слияния обнаружено расхождение между историческим SHA ADR-081 и исходным кодом после исправления Ruff;
- старый ADR и замороженный пакет сохранены без переписывания;
- добавлен ADR-082, отдельный реестр исходных файлов и закрывающаяся при ошибке проверка собственной идентичности;
- запрос выполнения отложен до повторной полной проверки, слияния исправления, постоянного файла владения v2 и устойчивой квитанции отрицательного исхода;
- `decision marker`: `ADR-082-stage3b-qwake-lc4-e-runtime-operation-identity-repair`.

### 30 июля — подготовка постоянной доказательной цепочки v2 `QW-LC4-E`

- PR №143 слит коммитом `5e61ed650c9beda2cde1f58650345f01694836f6` и независимо проверен после слияния;
- добавлен ADR-083 и канонический пакет подготовки с полной квитанцией проверки;
- шаблон постоянного файла владения v2 связывает актуальные идентичности авторизации вызова, авторизации выполнения, проверки перед выполнением, операции рабочей среды и восстановления идентичности, а также образ, Torch2PC, каталог результатов, время требования и `invocation_count=1`;
- контракт устойчивой квитанции исхода хоста покрывает отказ до запуска, ошибку создания процесса, код возврата, тайм-аут или сигнал, хеши полных потоков `stdout`/`stderr`, общий и сохранённый объём, согласованный признак усечения, снимки каталога результатов до и после и `retry=false`;
- запись, привязка полномочия, проверка образа, Docker и [локальное вычисление](../glossary.md#term-local-compute) не выполнялись;
- `decision marker`: `ADR-083-stage3b-qwake-lc4-e-persistent-evidence-chain-v2`.

### 2026-07-30 — реализация постоянной доказательной цепочки v2 `QW-LC4-E`

После слияния PR №144 открыт отдельный срез реализации от
`3d092440b0314f02072c9773cc91018bf2860744`. Реализованы только закрытые при
ошибке интерфейсы записи для файла владения v2 и терминального исхода хоста:
эксклюзивность без перезаписи, режим `0600`, `fsync` файла и каталога, запрет
символьного родительского каталога и проверка точных сохранённых байтов.
Подключение к исполнителю и фактическое выполнение остаются закрытыми.

### 2026-07-30 — привязка исполнителя хоста к постоянному файлу владения v2

После независимой проверки слияния PR №145 открыт отдельный срез от `0303a1514e2875a057ef1b20293a01b36a9c6b2b`. Добавлен вход, привязанный к файлу владения, который требует точный сохранённый файл владения v2, ограничивает делегирование одним запуском, вычисляет хеши полных потоков и записывает устойчивую терминальную квитанцию. Рабочий файл владения, образ, команда и Docker не материализованы.

### 2026-07-30 — подготовка финального подтверждения `QW-LC4-E`

- зафиксирована независимая проверка слияния PR №146 на `2957d8f6975c88e7bdb23243e3915c7f51d4ba47`;
- зафиксированы `39` направленных, `240` расширенных и `1287` полных тестов при `14` предупреждениях;
- открыт статический срез подготовки подтверждения на точном коммите слияния;
- контракт требует точную операторскую фразу, идентичность оператора, время по всемирному координированному времени после слияния и `invocation_count=1`;
- выпуск подтверждения, файл владения, исход, потребление авторизации, проверка образа, команда, Docker и выполнение остаются закрытыми.


### 2026-07-30 — подготовка выпуска финального подтверждения `QW-LC4-E`

- независимо проверено слияние PR №147 на `eb20c157584efff8e9aa0418385242c7d7b26eab`;
- зафиксированы `50` направленных, `251` расширенный и `1298` полных тестов при `14` предупреждениях;
- открыт статический срез подготовки выпуска на точном коммите слияния;
- контракт требует отдельные идентичности оператора и выпускающего, упорядоченные времена, единственный путь, атомарную запись без перезаписи, режим `0600` и `fsync`;
- реализация записи, подтверждение, файл владения, исход, авторизация, образ, команда, Docker и выполнение остаются закрытыми.


### 2026-07-30 — реализация выпуска финального подтверждения `QW-LC4-E`

- независимо проверено слияние PR №148 на `8343724c66b1d22f01846d9fc70f01738a09127a`;
- зафиксированы `61` направленный, `262` расширенных и `1309` полных тестов при `14` предупреждениях;
- открыт отдельный срез реализации на точном коммите слияния;
- реализован атомарный механизм записи без перезаписи с режимом `0600`, `fsync`, проверкой цепочки родительских каталогов, оставшихся временных файлов и точных сохранённых байтов;
- производственная точка вызова, подтверждение, файл владения, итоговая квитанция, проверка образа, команда, Docker и выполнение отсутствуют.


### 2026-07-30 — подготовка материализации финального подтверждения `QW-LC4-E`

- независимо проверено слияние PR №149 на `31206012ef7cbd2b7b21a2017374c11123abd42c`;
- зафиксированы `79` направленных, `280` расширенных и `1327` полных тестов при `14` предупреждениях;
- открыт статический срез подготовки на точном коммите слияния;
- контракт связывает оператора, выпускающего и материализующего субъекта, упорядоченные времена по всемирному координированному времени, точный механизм записи, путь и SHA-256;
- производственная точка вызова, подтверждение, файл владения, исход, образ, команда, Docker и выполнение отсутствуют.


### 2026-07-30 — реализация материализации финального подтверждения `QW-LC4-E`

- независимо проверено слияние PR #150 `6497cd904f9403622249c5a32f08ef6e8bb11532`;
- от точного коммита слияния открыт отдельный ограниченный этап реализации;
- реализована одна явно вызываемая функция материализации с одной передачей операции модулю записи и одной проверкой результата после записи;
- рабочая точка вызова, подтверждение, маркер владения выполнением, итоговая квитанция, проверка образа, команда, Docker и выполнение отсутствуют.

### 2026-07-30 — подготовка вызова материализации финального подтверждения `QW-LC4-E`

- независимо проверено слияние PR №151 на `7d5e5058af6a845cf4a6add2e7fe199894f48b24`;
- зафиксированы `108` направленных, `309` расширенных и `1356` полных тестов при `14` предупреждениях и четыре успешные обязательные проверки непрерывной интеграции;
- открыт чистый срез подготовки единственного будущего вызова модуля материализации;
- прямой вызов модуля записи, автоматическая и слепая повторная попытка запрещены; явное восстановление требует предварительной проверки устойчивого состояния;
- модуль материализации, модуль записи, подтверждение, файл владения, квитанция исхода, образ, команда, Docker и выполнение отсутствуют.

### 2026-07-31 — реализация адаптера вызова материализации финального подтверждения `QW-LC4-E`

- независимо проверено слияние PR №152 на `febfba65d2f200fd2163928643eadd807a6b4d21`;
- зафиксированы `124` направленных, `325` расширенных и `1372` полных теста при `14` предупреждениях и четыре успешные проверки непрерывной интеграции;
- реализована предварительная проверка устойчивого состояния и не более одного делегирования модулю материализации;
- корректное существующее подтверждение считается успехом без повторного вызова, некорректное завершает операцию закрыто при ошибке;
- производственная точка вызова, подтверждение, файл владения, квитанция исхода, Docker и выполнение отсутствуют.

### 2026-07-31 — `QW-LC4-E`: подготовка операторской операции вызова материализации

- независимо проверен PR №153, слитый как `0ace9f1025100fa29ff0af7523fde17674c4852b`;
- зафиксированы `144` направленных, `345` расширенных и `1392` полных теста при `14` предупреждениях и четыре успешные проверки CI;
- открыт чистый срез подготовки операции, привязанный к точному будущему вызову и отдельной фразе действия;
- отдельная предварительная проверка, прямые вызовы адаптера вне будущей реализации, материализатора и модуля записи запрещены;
- операция, подтверждение, файл владения, исход и выполнение отсутствуют.


### 2026-07-31 — `QW-LC4-E`: реализация операторской операции вызова материализации

- независимо проверен PR №154, слитый как `5ee6d2346e558be19cfdf79e8a77b0568475bf4c`;
- зафиксированы `162` направленных, `363` расширенных и `1410` полных тестов при `14` предупреждениях и четыре успешные проверки CI;
- реализована библиотечная функция проверки операции и единственной передачи точному адаптеру;
- отдельная предварительная проверка и прямые вызовы материализатора или модуля записи запрещены;
- производственная точка вызова, операция, подтверждение, файл владения, исход и выполнение отсутствуют.


## 2026-07-31 — этап 4-E: контракт производственной точки вызова операции

После проверки PR №155 после слияния открыт отдельный подготовительный срез. Зафиксированы
точный путь будущего командного интерфейса, явный канонический файл операции, единственный вызов
библиотечной операции и закрытая при ошибке семантика результата. Производственная точка
вызова не добавлена; операция не выполнена.

### 2026-08-01 — `QW-LC4-E`: реализация производственной точки вызова операции

- PR №162 проверен после продвижения `main`: первый родитель `dc8dc200515959858d43b68984dbd87f27f3446c`, слияние `b27e252cf7c64e88d5d61bf7a23c70ffc5957959`, разность по первому родителю `18/1516/0`;
- добавлен единственный командный интерфейс из ADR-095 с обязательными `--project-root` и `--operation-json`;
- вход должен быть точным каноническим `JSON`; делегирование библиотечной операции ограничено одним вызовом;
- результат проверяется и выводится канонически только при успехе; файл результата не создаётся;
- поведенческие эффекты тестируются только в изолированных временных копиях;
- в основном рабочем дереве операция, подтверждение, файл владения, квитанция исхода, Docker и локальное вычисление отсутствуют.



## 2026-08-01 — этап 4-E: контракт выполнения производственной точки вызова

После проверки PR №163 после слияния открыт отдельный подготовительный срез. Зафиксированы будущая отдельная авторизация, точный канонический файл операции, одноразовый набор аргументов без оболочки и повторная проверка динамических предусловий. Производственная точка вызова не выполнялась.

### 2026-08-02 — `QW-LC4-E`: авторизация выполнения производственной точки вызова

- независимо проверено слияние PR №164 на `75936adac9ee100f9538f5af13a8ce312642ee0b`;
- зафиксированы `240` направленных, `441` расширенный и `1488` полных тестов при `14` предупреждениях и четыре успешные проверки CI;
- материализованы отдельная однократная запись разрешения и канонический предварительно сформированный объект операции;
- разрешение связано с оператором, точной точкой вызова, SHA-256 входа и единственной будущей попыткой без оболочки и повторов;
- до независимой проверки будущего слияния разрешение не вступает в силу и не потребляется; операция, подтверждение и локальное вычисление отсутствуют.

### 2026-08-02 — `QW-LC4-E`: переход после финального подтверждения

- проверен пятифайловый пакет доказательных материалов выполнения производственной точки вызова материализации подтверждения;
- подтверждены потребление разрешения, завершение единственной попытки, материализация подтверждения и запрет повтора;
- в проверенном пакете отсутствуют постоянный файл владения v2, терминальная квитанция хоста и выход выполнения;
- пакет классифицирован как завершение линии подтверждения, но не как инженерный отчёт расширения;
- `QW-5`, `C1/C2/C3/R`, тестовая выборка и публикация остаются закрытыми;
- следующий допустимый срез — отдельная подготовка допуска фактического одноразового инженерного запуска через связанный с файлом владения исполнитель хоста.

### 2026-08-03 — `QW-LC4-E`: фиксация области подготовки допуска финального инженерного вызова

- после независимой проверки слияния PR №167 открыта отдельная ветка фиксации области;
- зафиксированы точные идентичности перехода после подтверждения, финальных доказательных материалов, постоянной доказательной цепочки v2 и привязанного к файлу владения исполнителя;
- единственной перспективной точкой будущего инженерного вызова назначена `invoke_lease_bound_host_runtime`;
- исторические авторизации и прямая операция разрешены только как входы проверки происхождения и не могут использоваться повторно;
- текущий срез не создаёт схему допуска, программу проверки, тесты допуска, запись допуска, авторизацию, файл владения, исход хоста или выход выполнения;
- `QW-5` и научная кампания остаются закрытыми.

## 2026-08-03 — подготовка допуска финального инженерного вызова

- добавлен [ADR-101](../decisions/ADR-101-stage3b-qwake-lc4-e-final-engineering-invocation-admission-authoring.md);
- материализованы чистая схема, программа проверки и каноническая запись допуска;
- отрицательные проверки изменяют только временные копии;
- авторизация, файл владения v2, устойчивый исход хоста, выход выполнения и `QW-5` остаются закрытыми.

## 2026-08-03 — репозиторная печать допуска финального инженерного вызова

- независимо проверено слияние PR №169 на `d2539eb440e758c1f29b935f8599561bec7126bc` с точной головой `b81c11971f1e9b78e59dd39c4d182722a3001044`;
- подтверждены двухкоммитный граф, граф слияния, область из 17 файлов, проверки финальной головы, `ruff`, четыре статические проверки и 23 направленных теста;
- материализована двухфайловая репозиторная квитанция, связывающая запись допуска и связанные с ней программные поверхности с точным состоянием `main`;
- квитанция ещё не завершена до собственного слияния и независимой проверки после слияния;
- новая авторизация, фраза оператора, команда вызова, файл владения v2, устойчивый исход хоста, выход среды выполнения и `QW-5` остаются закрытыми.

## 2026-08-03 — фиксация области подготовки новой одноразовой авторизации

- независимо проверено слияние PR №170 на `a5b96edb1f82485561e0f52d6a98432d55ae8609` с точной головой `6201997a71428bdb873d95d76c5b0882be532b2a`;
- подтверждены однокоммитный граф, граф слияния, область из 14 файлов, проверки финальной головы, `ruff`, четыре статические проверки и 27 направленных тестов;
- завершённость репозиторной печати выведена из неизменяемой квитанции, точного слияния и независимой проверки после слияния;
- зафиксированы точные входы, будущие программные поверхности, отдельность и одноразовость новой авторизации;
- текущий срез не создаёт схему, программу проверки, тесты, запись авторизации, фразу оператора, команду, файл владения, исход хоста или выход выполнения;
- `QW-5`, тестовая выборка и публикация остаются закрытыми.

## 2026-08-03 — подготовка новой одноразовой авторизации финального инженерного вызова

- независимо проверено слияние PR №171 на `61f190db2fbd4bf0ee8a58cac8b6841fbecc6cdd` с точной головой `6093a18156036d8aa470c88844b0580cd3926c4e`;
- зафиксированы однокоммитный граф, граф слияния, область из 13 файлов, проверки финальной головы, `ruff`, четыре статические проверки и 27 направленных тестов;
- добавлен [ADR-104](../decisions/ADR-104-stage3b-qwake-lc4-e-final-engineering-invocation-authorization-record-authoring.md);
- материализованы чистая схема, программа проверки, тесты и каноническая запись, связанная с оператором `local-posix-account:dzmitry-prychyna`;
- запись выпущена, но до собственного слияния и независимой проверки после слияния остаётся неэффективной;
- команда, потребление, файл владения, исход хоста, выход выполнения и `QW-5` отсутствуют.

## 2026-08-03 — фиксация области подготовки попытки потребления авторизации

- независимо проверено слияние PR №172 как `47bb24dc8fa95292be33428ba8bc7ee598c49b1e` с точной головой `220ce31235a28d1583f90cde5acd8f87ca5c2752`;
- подтверждены однокоммитный граф, граф слияния, область из 17 файлов, точные SHA-256, одиннадцать пакетов, `ruff`, четыре статические проверки и 37 направленных тестов;
- выведено эффективное, проверенное после слияния, но непотреблённое состояние одноразовой авторизации;
- добавлен [ADR-105](../decisions/ADR-105-stage3b-qwake-lc4-e-final-engineering-invocation-authorization-consumption-attempt-scope-freeze.md);
- зафиксированы будущие поверхности записи попытки, атомарное потребление с началом попытки и эксклюзивным владением v2, а также запрет повторов;
- запись попытки, потребление, команда, владение, исход хоста, выход выполнения и `QW-5` отсутствуют.

## 2026-08-03 — подготовка записи попытки потребления авторизации

- независимо проверено слияние PR №173 как `28b4627436244893195231f55f2d0d5fb2d1062e` с точной головой `17af7d6f4473af846f2d293192082074cad99cf2`;
- подтверждены однокоммитный граф, граф слияния, область из 13 файлов, точные SHA-256, двенадцать пакетов, `ruff`, четыре статические проверки и 37 направленных тестов;
- добавлен [ADR-106](../decisions/ADR-106-stage3b-qwake-lc4-e-final-engineering-invocation-authorization-consumption-attempt-record-authoring.md);
- материализованы чистая схема, программа проверки, тесты и отдельная каноническая запись попытки;
- запись подготовлена, но авторизация остаётся непотреблённой, а попытка — неначатой;
- команда, владение v2, исход хоста, выход выполнения и `QW-5` отсутствуют.

## 2026-08-03 — фиксация области атомарного перехода потребления авторизации

- независимо проверено слияние PR №174 как `5890c755fcf5aa1ae6651f3b592705457b9a9b91` с точной головой `bc153fb14eb73b18353739cacb5def31a8f4c70a`;
- подтверждены однокоммитный граф, граф слияния, область из 17 файлов, точные SHA-256, тринадцать пакетов, `ruff`, четыре статические проверки и 47 направленных тестов;
- подготовленная запись попытки признана проверенной после слияния при непотреблённой авторизации и неначатой попытке;
- добавлен [ADR-107](../decisions/ADR-107-stage3b-qwake-lc4-e-final-engineering-invocation-authorization-consumption-attempt-atomic-transition-scope-freeze.md);
- единственной точкой устойчивой фиксации будущего перехода назначено атомарное неперезаписывающее создание точных устойчивых байтов владения v2;
- текущий срез не создаёт реализацию перехода, не потребляет авторизацию, не начинает попытку, не создаёт владение, не вызывает среду выполнения и не открывает `QW-5`.
