# Research log: July 2026

[Русская версия](2026-07.md)


## 2026-07-10

- Converted the dissertation package into a GitHub-ready research repository.
- Separated code, [configuration](../glossary_EN.md#term-configuration), analysis notebooks, thesis, article, and
  literature metadata.
- Preserved the original notebook as a migration [baseline](../glossary_EN.md#term-baseline).
- Added the controlled Ubuntu/ROCm environment for RX 7700 XT.
- Defined milestones from control gate to publication artifact.

## 12–13 July 2026

- Pinned Torch2PC to `00c6c50ee3540537bbb56ab2b6567b541f42b093`.
- Built and validated the controlled ROCm image on the RX 7700 XT.
- C0 and C1 satisfied their pre-specified CPU/GPU thresholds.
- After correcting grid enumeration, all 96 pilot cells completed.
- The pilot did not evaluate test data and registered no failed cells.
- Selected FixedPred `eta=0.1`, `n=10` and Strict `eta=0.05`, `n=20`.
- The planning calculation advised 10 paired final seeds; this is advisory and does not establish
  confirmatory power.

## Next action

Preserve compact pilot observations and provenance, then rebuild and lock the
selected environment, repeat C0/C1, and create `pilot-freeze`.

## 2026-07-13 — post-pilot final freeze preparation

ADR-005 was accepted before any final-test access. The final pair count remains
10 under the pilot advisory rule. The repository now defines an immutable
80-cell final plan, deterministic method counterbalancing, safe resumption, a
separate pilot-registry snapshot, a combined [configuration](../glossary_EN.md#term-configuration) hash, and GPU
telemetry. Hyperparameters, the primary metric, the equivalence margin, and the
primary contrasts remain unchanged.

## 13 July 2026 — Stage 1 and Stage 2 completion

- Stage 1 completed: 80/80 confirmatory cells, 0 failed.
- Stage 2 pinned Torch2PC commit
  `b20d9142e4bdbf57b3ec8bf9f9c4472372ec8db4`.
- CPU float64 and GPU float32 equivalence gates passed.
- Stage 2 completed: 80/80 confirmatory cells, 0 failed.
- Paired Stage 1/Stage 2 test accuracy and macro-F1 values matched.
- Exact approached BP [runtime](../glossary_EN.md#term-runtime); FixedPred and Strict became substantially faster.
- Stage 2 reports, completion snapshot, results manifest, and cross-version
  analysis were generated.
- An audit documents the Torch2PC changes and the scope of demonstrated
  equivalence.

## 13 July 2026 — post-experiment maintenance

Only tooling and publication provenance are changed: LF-stable CSV writing, the
correct Stage 2 environment-lock binding in the Stage 2 manifest, and
module-based [execution](../glossary_EN.md#term-execution)-plan generation. The [execution](../glossary_EN.md#term-execution) lock and all 80 run
results remain unchanged.

## 13 July 2026 — post-experiment maintenance completed

- Stage 1 and Stage 2 snapshot checksums were verified against the actual files.
- The Stage 2 results manifest is bound to the Stage 2 environment lock and no
  longer includes itself.
- The Stage 2 [execution](../glossary_EN.md#term-execution)-plan generator now runs as a Python module.
- Ruff and the regression suite pass, with 63 successful tests.
- The next publication step is to create [execution](../glossary_EN.md#term-execution)/results-state tags and a
  separate replication bundle containing the raw run artifacts.



## 2026-07-13 — extended Stage 3 design

- Stage 1/2 were fixed as an immutable [baseline](../glossary_EN.md#term-baseline).
- Core axes are locality, exact VJP organization, adaptive stopping, and
  periodic VJP refresh.
- Fixed random feedback remains a conditional exploratory track.
- The Stage 3 protocol, ADRs, design contract, locality schema, and [scaling](../glossary_EN.md#term-scaling)
  model family were added.
- [Execution](../glossary_EN.md#term-execution) remains blocked until candidates, gates, and freeze exist.

## 16 July 2026 — B1/B2 preregistration

- Froze separate B1 `isolated_layer_vjp` and B2 `composite_vjp` contracts.
- B1 is implemented first; B2 remains closed until sealed `EQ-B1`.
- Direct B1/B2 control and zero dangerous admissions are preregistered.
- Primary timing and structural counters are separated.
- B1/B2 are explicitly separated from estimator, offline policy, hysteresis,
  and `QWake-PC`.
- After `EX-IF0`, the plan includes `A11-OFF0`, `A11-OFF1`, predictor
  preregistration, and shadow control.
- Implementation, ROCm execution, and [evidence](../glossary_EN.md#term-evidence) are absent.

## 2026-07-24 — full `QWake` refactor before a new image

- retired the `QW-4B-F-v1` single-[attempt](../glossary_EN.md#term-attempt) authorization [candidate](../glossary_EN.md#term-candidate) before
  [execution](../glossary_EN.md#term-execution);
- preserved its preflight, receipt, authorization, and logs in an external audit
  directory;
- confirmed the absence of engineering
  [evidence](../glossary_EN.md#term-evidence) and scientific execution;
- rejected a local terminology fix as insufficient;
- rebuilt the active plan, roadmap, future-policy boundary, glossary, and status
  around `R/M/Γ/C` and the `LOCAL_COMPUTE` family;
- replaced the old active `QW-6`–`QW-10` labels with `C1/C2/C3/R` roles;
- required a new immutable image, new preflight, and new single-attempt
  authorization after merge.

### `QW-4B-DOC-R1` documentation-test migration validation

Strict language-structure, glossary, local-link, and both documentation builds
passed. The full test suite found six historical checks that still treated the
`QW-0…QW-10` sequence as active and required old markers in the fully refactored
plan.

Rather than restoring two competing roadmaps, the checks now separate preserved
`QW-2/QW-3/QW-4` history from the active
`QW-4B-DOC-R1 → QW-4B-F-v2 → QW-4B-E-v2 → QW-LC → QW-5 → C1/C2/C3/R`
sequence. The active plans also regain exact `P0/P1/P2` pairs, closed execution
markers, and the strictly offline `C2` contract.

### `QW-4B-F-v2`: new runtime freeze without execution

- the new image was built from merge commit `e413bb1e13cee42f702512e499f994e90df21e45` and binds Torch2PC `b20d9142e4bdbf57b3ec8bf9f9c4472372ec8db4`;
- 17 static and unit checks passed again, including strict bilingual documentation builds;
- the live preflight bound CPU/float64 and ROCm/float32 to `sha256:bd91fab26df5f91a3aba90b8cad38badccab3a1a7bfb20efe4126a88a13236c4`;
- an authorization for six P0/P1/P2 cells, one synthetic batch, seed 0, and one attempt was issued and independently reverified;
- eight external files were preserved byte for byte, while two SHA-256 registries bind the source and complete frozen package;
- the execution command was not called, the output root is absent, and engineering evidence and science remain closed;
- the next separate slice is `QW-4B-E-v2`; `QW-LC0` remains closed.

### `QW-4B-E-v2`: single execution and recovery adjudication

- the single authorized attempt ran from isolated source commit
  `e413bb1e13cee42f702512e499f994e90df21e45`; runtime was not repeated;
- all six `CPU/ROCm × P0/P1/P2` cells completed, the output contains exactly six
  files, and report
  `sha256:54dba01d47814dc00fa53bd69c00865bd1c47754c017c7482c895162d3a86b82`
  binds the result;
- the original completion receipt retains the wrapper failure after successful
  computation, and both failed recovery audits are preserved;
- recovery-v3 verified authorization JSON/model equivalence, both lanes,
  observation nesting, oracle isolation, and zero disabled-capability effects;
- the exact output, 21-file audit package, and two-file external seal were
  materialized, for 29 files total;
- the authorization is consumed, retry is prohibited, evidence remains
  engineering-only, and publication and `QW-LC0` remain closed pending the
  repository seal.


### `QW-4B-E-v2` post-merge transition to `QW-LC0`

- PR #110 was merged by merge commit `4f23b752a40ae05de9fc7ee49c9962c44083b71d`, preserving
  repository-seal commit `26bc0ef635e13dba719d3356fe17382f0037d1df` as the second parent;
- the exact output, audit package, and external seal passed SHA-256 and
  semantic integrity checks again on `main`;
- the repository evidence is admitted as sealed;
- `QW-LC0` is open only for a separate semantics-and-scope freeze;
- `LOCAL_COMPUTE` implementation and execution, the scientific image,
  C1/C2/C3/R, the test split, and publication remain closed.


### `QW-LC0` semantics-and-scope freeze

- sealed contract `stage3b-qwake-lc0-semantics-scope-v1` with explicit `R/M/Γ/C` separation;
- bounded `LOCAL_COMPUTE` to `LOCAL_SWEEP` and `ANALYTIC_COMPLETION`;
- bounded the first candidate to `FixedPred`, `eta=1`, `lenet_classic`, and
  `stage2_baseline`;
- deferred the exact response to `QW-LC1` and the resource/cost model to
  `QW-LC2`;
- kept implementation, execution, the scientific campaign, test split, and
  publication closed.

### `QW-LC0` repository freeze

- contract `stage3b-qwake-lc0-semantics-scope-v1` was merged into `main`
  `8429f54257685a879b0a44499d5fa81eab7310ea`, preserving contract commit
  `715308451ac3e696d4c2209276d36853f6799d6f` as the second parent;
- the 22-file tree, contract, and registry were reverified unchanged;
- a two-file repository-state receipt was materialized;
- transition to `QW-LC1`, implementation, execution, feature collection,
  oracle-label generation, the test split, and publication remain closed until
  the receipt is merged and independently reverified.

### Transition to `QW-LC1`

- the `QW-LC0` repository freeze was merged into `main`
  `0fbd54be337665e06ad63b6d9c7f8ca978ab75ee` and independently reverified;
- the two-file `stage3b-qwake-lc1-transition-v1` receipt was materialized;
- the next slice is limited to the `R(a,s)` schema, mandatory observables,
  and `~R`;
- `QW-LC1`, `Γ`, cost, implementation, execution, feature collection,
  oracle labels, the test split, and publication remain closed pending merge.
### `QW-LC1` required-response schema

- the `QW-LC1` transition was merged into `main` `c3533fcb63ffc869faddbaa99645c9099d16d1cc` and
  independently reverified;
- contract `stage3b-qwake-lc1-required-response-schema-v1` was sealed;
- `R(a,s)` is limited to named parameter gradients, [endpoint](../glossary_EN.md#term-endpoint) beliefs, and
  scalar loss;
- canonical serialization separates the JSON manifest from source tensor
  payloads;
- mandatory observables freeze structure, finiteness flags, and SHA-256;
- `~R` uses an exact structural gate and per-entry zero-safe numerical checks
  without assuming transitivity;
- CPU/float64 and ROCm/float32 profiles are frozen, with ROCm remaining
  decision-facing;
- state/RNG/[fallback](../glossary_EN.md#term-fallback) are deferred to `QW-LC3`, and `Γ`, `Φ`, `C`, and `~C`
  to `QW-LC2`;
- implementation, execution, feature collection, oracle labels, test split,
  and publication remain closed.
### `QW-LC1` repository freeze

- schema `stage3b-qwake-lc1-required-response-schema-v1` was merged into `main` `59e3143ba105a5b298e2cd551b221b8f6dae96f7`, preserving commit `de2b5a37583b22946073390caa244bee35dd793b` as the second parent;
- the exact 22-file scope, schema tree, contract, and registry were reverified unchanged;
- a two-file repository-state receipt was materialized;
- `QW-LC1` remains incomplete, while transition to `QW-LC2`, [resource trajectory](../glossary_EN.md#term-resource-trajectory), cost, implementation, execution, and publication remain closed pending merge and separate receipt verification.

### Transition to `QW-LC2`

- the `QW-LC1` repository freeze was merged into `main` `9d073bc3c90eeda53ca03d0f7762b65da8749269` and
  independently verified;
- `QW-LC1` is complete and transition to `QW-LC2` is permitted;
- the two-file `stage3b-qwake-lc2-transition-v1` receipt was materialized;
- the next contract is limited to the `Γ(a,s)` schema, `Φ`, and `~C`;
- fields, units, cost, matched validation, implementation, execution, the test
  split, and publication remain closed pending merge.

### `QW-LC2` resource and cost contract

- the `QW-LC2` transition was merged into `main` `858403cbb2423ad3427ab7a042266880ca34c0b7` and
  independently verified;
- contract `stage3b-qwake-lc2-resource-cost-contract-v1` was materialized;
- ordered `Γ`, no-double-counting `Φ`, an 11-field `C`, an engineering shadow
  profile, and a future end-to-end profile were frozen;
- `~C` is fieldwise with CPU/ROCm tolerances and no transitivity assumption;
- Pareto applies only after `~R` admission, ambiguity uses the registered
  tie-break, and an incomplete vector selects `LOCAL_SWEEP`;
- `QW-LC3`, implementation, execution, the test split, and publication remain
  closed.

### `QW-LC2` repository freeze

- PR #118 was merged into `main` `8f24229bcf19736086fe6f0340bda26dd533936a` and independently reverified;
- the exact parents and tree of `3f1682765089b0819dcaaf9bb449c4c1bd155142` were preserved;
- receipt `stage3b-qwake-lc2-repository-freeze-v1` was materialized;
- `QW-LC2` is incomplete pending receipt merge; `QW-LC3`, implementation,
  execution, the test split, and publication remain closed.

### Transition to `QW-LC3`

- the `QW-LC2` repository freeze was merged through PR #119 into `main`
  `4f7c533047214398e7ec4dde9d58b5fc06964b90` and independently verified;
- freeze commit `3f4310a05de5b7cd3db0cdb5c8f7cf4bbcb09150` is preserved, the tree is
  unchanged, and the repository receipt and resource contract match their
  expected SHA-256 values;
- `QW-LC2` is complete and transition to `QW-LC3` is permitted;
- two-file receipt `stage3b-qwake-lc3-transition-v1` was materialized;
- future contract scope is limited to matched shadow validation, shared state,
  RNG restoration, the complete exact-reserve suffix, and repeat aggregation;
- `QW-LC3`, implementation, execution, feature collection, oracle labels, the
  test split, and publication remain closed until transition merge.

### `QW-LC3` matched shadow-validation contract

- the `QW-LC3` transition was merged through PR #120 into `main`
  `a7e0c4ec1978042d68abc7437e3005e4295e75ff` and independently verified;
- contract `stage3b-qwake-lc3-matched-shadow-validation-contract-v1` was
  materialized;
- canonical `opaque_state_ref`, disposable shared-snapshot forks, and exact
  restoration of the complete RNG inventory are frozen;
- twelve matched repeats with equal counts of the two arm orders and no repeat
  exclusion are defined;
- two forced probes validate the complete exact-reserve suffix before the first
  and after the final repeat;
- cost is aggregated componentwise over paired differences without
  scalarization or statistical-significance claims;
- implementation, execution, feature collection, oracle labels, test data, and
  publication remain closed.

### `QW-LC3` repository freeze

- contract `stage3b-qwake-lc3-matched-shadow-validation-contract-v1` was merged through PR #121 into `main` `71e73f56408c720334b8fa03e7133762c8bbcc43` and independently verified;
- the exact parents and tree of `fb3f1cd4a4d3b4261db1179badcc1ccacddfe936` were preserved;
- receipt `stage3b-qwake-lc3-repository-freeze-v1` was materialized;
- `QW-LC3` is incomplete pending receipt merge; `QW-LC4-I`, implementation, execution, feature collection, oracle labels, the test split, and publication remain closed.

### `QW-LC4-I` bounded implementation

- PR #122 repository freeze was merged into `main`
  `7c6cbb6ba4941cf78b2bfec3e6e8955c2830a58b` and independently verified;
- `QW-LC3` is complete and bounded implementation authoring is permitted;
- module `stage3b_qwake_lc4_bounded.py` materializes the eta=1 wavefront
  completion and complete exact suffix from disposable shared-state forks;
- canonical state/RNG controls, `~R`, `Γ -> C`, reserve probes, the twelve-pair
  schedule, and componentwise aggregation are implemented;
- only synthetic unit tests are callable; runtime execution, scientific data,
  feature collection, oracle labels, policy activation, and publication remain
  closed pending `QW-LC4-F`.

### `QW-LC4-F` runtime-freeze authoring

- `QW-LC4-I` merged through PR #123 and was independently verified on `main` `c9f3dadcd5330887584b8bf71d906c667dacf076`.
- Request `stage3b-qwake-lc4-f-runtime-freeze-request-v1` is frozen: two lanes, candidate indices `0..6`, 12 repeats per cell, 168 matched-pair cells, and 28 reserve probes.
- A runtime frontier adapter, deny-all preflight, one-attempt authorization schema, and executor-free sealing script are added.
- The image digest is deliberately absent until the authoring commit exists; the image must then be built from that exact commit.
- Runtime execution, engineering evidence, scientific execution, the test [dataset](../glossary_EN.md#term-dataset), publication, and policy activation remain closed.
### `QW-LC4-F` runtime freeze

See [ADR-063](../decisions/ADR-063-stage3b-qwake-lc4-f-runtime-freeze_EN.md).

- the verified image was frozen from the exact authoring commit;
- CPU/ROCm checks and all 22 static checks passed;
- one-attempt authorization covers 168 matched cells;
- the ten-file `stage3b-qwake-lc4-f-runtime-freeze-v1` package was
  materialized;
- `image-build.log` explicitly records reconstructed provenance for the
  original image; a separate rebuild is not identity evidence;
- no execution occurred, `QW-LC4-F` remains incomplete pending merge, and
  `QW-LC4-E` plus scientific capabilities remain closed.
### `QW-LC4-E` admission authoring

See [ADR-064](../decisions/ADR-064-stage3b-qwake-lc4-e-execution-admission-authoring_EN.md).

- the branch was opened from verified merge commit `453bb4eb...`;
- the schema reverifies the exact `QW-LC4-F` package;
- admission requires a separate operator acknowledgement and absent output root;
- no admission record, lease, executor, or engineering evidence exists;
- actual execution remains closed.
### `QW-LC4-E` admission freeze

See [ADR-065](../decisions/ADR-065-stage3b-qwake-lc4-e-execution-admission-freeze_EN.md).

- admission authoring merged through PR #125 into `main` `bce821dff0729629db0ccb306d8f3fd1dd9a2e13` and was independently verified;
- a five-file admission package was materialized;
- the record binds the exact control-plane commit and permits one attempt;
- absence of the output root and lease was verified;
- the branch-level gate, executor, evidence, [test-dataset access](../glossary_EN.md#term-test-dataset-access), and publication remain closed.
### `QW-LC4-E` lease and wrapper contract authoring

See [ADR-066](../decisions/ADR-066-stage3b-qwake-lc4-e-execution-lease-wrapper-authoring_EN.md).

- the admission freeze merged into `main` `12b7d24153a681f731a43e8497275016ad4e1656` and was independently verified;
- a pure prospective one-attempt lease schema was implemented;
- a wrapper effect contract for 168 cells and 28 reserve probes was implemented;
- a future claim must be atomic, exclusive, and non-retryable;
- no lease writer, runtime executor, result writer, or output root exists;
- the authorization remains unconsumed and execution has not started.

### Atomic `QW-LC4-E` lease/wrapper implementation

See [ADR-067](../decisions/ADR-067-stage3b-qwake-lc4-e-execution-lease-wrapper-implementation_EN.md).

- PR #127 merged into `main` as `e0455dc77b49f5b220231509fe6062d275b6ee9b` and passed independent verification;
- a separate module implements an exclusive no-replace lease claim;
- output absence is rechecked after the claim;
- the backend is confined to staging and complete output is promoted atomically;
- failure preserves the lease, prohibits retry, and removes incomplete staging;
- the synthetic verifier and unit tests leave repository effects absent;
- actual claim, execution, evidence, test data, and publication remain closed.
### `QW-LC4-E` execution-freeze authoring

See [ADR-068](../decisions/ADR-068-stage3b-qwake-lc4-e-execution-freeze-authoring_EN.md).

- PR #128 merged into `main` as
  `24966cd2a0380e46ab1924ff4ab8987f17e1fe9e` and passed independent
  verification;
- exact head `15588616c24d523f1c983fc205aeaae32a33958e`, both parents, the
  16-file tree, and CI 2/2 were confirmed;
- a pure execution-freeze request was materialized with SHA-256
  `sha256:9b28943043082efe96fb313f94875ef18c7f8e7361d8c0eb1b8c140e82a1e312`;
- it requires same-process claim+execute, no retry, atomic promotion, and a
  canonical backend receipt;
- the concrete backend and one-shot entrypoint are absent, so the execution
  freeze and lease remain unmaterialized;
- the next slice implements the concrete runtime backend without running it.

### `QW-LC4-E`: bounded runtime-backend implementation

See [ADR-069](../decisions/ADR-069-stage3b-qwake-lc4-e-runtime-backend-implementation_EN.md).

A separate branch was opened from merge commit
`49d691d497f4f719e82b271e9b9d441f9e4dfa63`. It materializes the concrete
backend and one-shot entrypoint without a lease or runtime execution. CPU
controls for all seven candidates passed `~R`; completed upper-wavefront
roundoff is approximately `1.4e-17..5.6e-17` and is handled by a pure bounded
canonicalization. A twelve-repeat control demonstrated that the order-effect
gate can be negative, so it is retained as a result instead of causing loss of
the single-attempt evidence. The immutable image and `execution-freeze-v1`
remain absent; execution is closed.

### `QW-LC4-E` execution-freeze materialization

See [ADR-070](../decisions/ADR-070-stage3b-qwake-lc4-e-execution-freeze-materialization_EN.md).

- PR #130 merged into `main` `67a084c0b970ad79ad0692442f660085a73b080a` and passed independent verification;
- immutable image `torch2pc-layerwise-thesis:0.1.0-qw-lc4-e-freeze-67a084c0b970` was built from that commit with identity `sha256:7da92b8f77f6dc37d42db832c5613ef6149dc488adc5b66465faa33e48ca021d`;
- the nine-file `execution-freeze-v1` package binds the image, backend, entrypoint, admission, and authorization;
- raw `image-build.log` bytes are preserved exactly and the single path is classified in `.gitattributes` as sealed binary evidence;
- the internal record enables the future one-shot entrypoint, but the branch-level execution gate remains closed;
- no lease, output root, engineering evidence, scientific execution, test-dataset access, or publication exists.

### QW-LC4-E one-shot engineering invocation authorization

- PR #131 was post-merge verified at `375db196b615f7024cd5f715de9c9be7b526a9f7`;
- a four-file authorization package for one future engineering invocation was materialized;
- semantic SHA-256: `sha256:0a60dacc1bfd5073cf52d76f2ec33ae54f00899aad9877d44607199659fda75a`;
- lease, output, execution, science, and publication remain closed.

### `QW-LC4-E` host invocation-wrapper authoring

See [ADR-072](../decisions/ADR-072-stage3b-qwake-lc4-e-one-shot-invocation-wrapper-authoring_EN.md).

- PR #132 merged into `main` as `8337d9ad0ac21a69a577ab74a73d05d69f8fa7a1` and passed independent verification;
- a pure canonical future host-wrapper contract was created;
- only frozen packages and Torch2PC read-only plus results read-write are permitted;
- `/dev/kfd`, `/dev/dri`, user/group and resource inputs, the exact command template, and a `/tmp` tmpfs are frozen;
- source-tree and dataset mounts, networking, privileged mode, and extra capabilities are forbidden;
- the host invoker, image inspection, invocation command, lease, and execution are absent.

### `QW-LC4-E` host invocation-wrapper implementation

See [ADR-073](../decisions/ADR-073-stage3b-qwake-lc4-e-one-shot-invocation-wrapper-implementation_EN.md).

- PR #133 merged into `main` `7cc17c6b36cb5115e63a2b64e4bff90a525b2465` and passed independent verification;
- exact local-image inspection is implemented through `docker image inspect` without pulling an image;
- the current identity is compared with the frozen record across tag, digest, layers, labels, source-commit environment, entrypoint, and working directory;
- canonical validation of thirteen resource inputs and deterministic in-memory argv materialization are implemented;
- the host invoker, lease, output, [evidence](../glossary_EN.md#term-evidence), scientific execution, test data, and publication remain absent.

### `QW-LC4-E` one-shot host-runtime-invoker authoring

See [ADR-074](../decisions/ADR-074-stage3b-qwake-lc4-e-one-shot-host-runtime-invoker-authoring_EN.md).

- PR #134 merged into `main` `be6486a9e3670343132f2c863a5a0cd5969ee9f6` and passed independent verification;
- a pure contract freezes one future child-container spawn;
- the sequence separates host revalidation from the container-owned atomic lease claim;
- authorization consumption starts only at successful lease claim and post-claim revalidation precedes computation;
- timeout, signal, bounded-output, and terminal no-retry rules are defined before implementation;
- the host invoker, lease, and [execution](../glossary_EN.md#term-execution) remain absent.

### `QW-LC4-E` one-shot host-runtime-invoker implementation

See [ADR-075](../decisions/ADR-075-stage3b-qwake-lc4-e-one-shot-host-runtime-invoker-implementation_EN.md).

- PR #135 merged into `main` `7f1655346bca77834d73a660c9857f1ff23b826c` and passed independent verification;
- double image and canonical-argv revalidation, one no-shell `Popen`, and a fixed host environment are implemented;
- the child receives a separate process group, `SIGINT`/`SIGTERM` forwarding, a terminal timeout, and bounded output;
- the host writes no lease and persists neither command nor logs;
- the verifier and tests never invoke Docker runtime, so execution and output remain absent.

### `QW-LC4-E` one-shot host-runtime-invoker repository freeze

See [ADR-076](../decisions/ADR-076-stage3b-qwake-lc4-e-one-shot-host-runtime-invoker-repository-freeze_EN.md).

- PR #136 merged into `main` `da51c8d858c541372525125640db99062041fc20` at `2026-07-29T15:27:45Z`;
- head `181abda36465d3a91db5970e684938266200a798`, both parents, and the 16-file tree are recorded in the receipt;
- the hermetic test correction remains a separate second commit without rewriting the original implementation;
- 2/2 CI checks, 139 targeted tests, and 1186 full tests passed, with 14 full-suite warnings;
- the two-file repository-freeze package is materialized;
- image inspection, `docker run`, lease claim, authorization consumption, execution, and output remain absent.

### 29 July — `QW-LC4-E` one-shot engineering invocation admission authoring

- after PR #137 merge verification, a separate invocation branch was opened from `3454d12d3cc16c9c50977e2a598e2bc1a8768441`;
- ADR-077 and a canonical admission package were added;
- the verifier rebinds repository freeze, authorization, and host invoker without runtime effects;
- `PREEXECUTION_IDENTITY_VERIFIED=false`, `DOCKER_RUN_PERFORMED=false`, and `LOCAL_COMPUTE_EXECUTION_OPEN=false`.
- decision marker: `ADR-077-stage3b-qwake-lc4-e-one-shot-engineering-invocation-admission`.

### 29 July — `QW-LC4-E` one-shot engineering invocation operation authoring

- after PR #138 merge verification, a separate `invocation-operation` branch was opened from `28be77706bc86abaf34f86e9bdcbdcb9cc2810a8`;
- ADR-078 and a canonical `operation` package were added;
- the record freezes exact dynamic inputs and future launch checks without runtime effects;
- `PREEXECUTION_IDENTITY_VERIFIED=false`, `INVOCATION_COMMAND_MATERIALIZED=false`, and `DOCKER_RUN_PERFORMED=false`;
- decision marker: `ADR-078-stage3b-qwake-lc4-e-one-shot-engineering-invocation-operation`.

### 29 July — `QW-LC4-E` one-shot engineering invocation execution authorization authoring

- after verification of the PR #139 merge, a separate `invocation-execution` branch was opened from `b0f6729e8fd1cb1aa172eef488dc56e36b335173`;
- ADR-079 and the canonical `execution-authorization` package were added;
- the record binds the merged operation, previous authorization, image, Torch2PC revision, and host invoker;
- the future same-process pre-execution verification contract is materialized without runtime effects;
- `PREEXECUTION_IDENTITY_VERIFIED=false`, `INVOCATION_COMMAND_MATERIALIZED=false`, and `DOCKER_RUN_PERFORMED=false`;
- `decision marker`: `ADR-079-stage3b-qwake-lc4-e-one-shot-engineering-invocation-execution-authorization`.

### 29 July — pre-execution contract for the `QW-LC4-E` one-shot engineering invocation

- after verification of the PR #140 merge, a separate `preexecution-verification` branch was opened from `49c4b97e93b47cefbf35576736927ece02c9402b`;
- ADR-080 and the canonical `preexecution-verification` package were added;
- the contract binds the merged authorization to the exact host invoker and forbids a separate dynamic verification outside the future calling process;
- the verifier confirms only static packages and implementation state without calling Docker;
- `PREEXECUTION_IDENTITY_VERIFIED=false`, `INVOCATION_COMMAND_MATERIALIZED=false`, `DOCKER_RUN_PERFORMED=false`;
- `decision marker`: `ADR-080-stage3b-qwake-lc4-e-one-shot-engineering-invocation-preexecution-verification`.

### 29 July — bounded `QW-LC4-E` one-shot engineering invocation runtime-operation authoring

- after verification of the PR #141 merge, a separate `runtime-operation` branch was opened from `494e6a0b2f10c26b49c90fbb84c23565699a4064`;
- ADR-081 and the canonical `runtime-operation` package were added;
- the entry point requires explicit permission, exact acknowledgement, a post-merge claim time, and the complete host-resource set;
- dynamic checks and the sole spawn are delegated to the unchanged `invoke_one_shot_host_runtime` without direct Docker calls in the new module;
- `PREEXECUTION_IDENTITY_VERIFIED=false`, `INVOCATION_COMMAND_MATERIALIZED=false`, and `DOCKER_RUN_PERFORMED=false`;
- `decision marker`: `ADR-081-stage3b-qwake-lc4-e-one-shot-engineering-invocation-runtime-operation`.

### 29 July — `QW-LC4-E` runtime-operation identity-repair authoring

- post-merge verification of PR #142 exposed drift between the historical ADR-081 SHA and the Ruff-corrected source;
- the old ADR and frozen package are preserved without rewriting;
- ADR-082 adds a separate source registry and fail-closed self-identity verifier;
- the execution request is deferred until corrected validation, repair merge, persistent lease v2, and a durable negative outcome;
- `decision marker`: `ADR-082-stage3b-qwake-lc4-e-runtime-operation-identity-repair`.

### July 30 — `QW-LC4-E` persistent evidence chain v2 authoring

- PR #143 was merged as `5e61ed650c9beda2cde1f58650345f01694836f6` and independently verified after merge;
- ADR-083 and a canonical authoring package with the complete validation receipt were added;
- the persistent lease-v2 template binds the current authorization/preexecution/runtime-operation/repair identities, image, Torch2PC, output root, claim time, and `invocation_count=1`;
- the durable host-outcome contract covers prelaunch/spawn failure, return code, timeout/signal, full-stream stdout/stderr hashes, total and captured byte counts, a consistent truncation flag, before/after output snapshots, and `retry=false`;
- persistence, capability wiring, image inspection, Docker, and [local compute](../glossary_EN.md#term-local-compute) were not performed;
- `decision marker`: `ADR-083-stage3b-qwake-lc4-e-persistent-evidence-chain-v2`.

### 2026-07-30 — QW-LC4-E persistent evidence chain v2 implementation

After PR #144 merged, a separate implementation slice was opened from
`3d092440b0314f02072c9773cc91018bf2860744`. Only fail-closed writers for lease
v2 and the terminal host outcome were implemented: exclusive no-overwrite,
mode `0600`, file/directory `fsync`, symbolic-parent rejection, and exact
persisted-byte verification. Runtime wiring and actual execution remain closed.

### 2026-07-30 — bind the host invoker to persistent lease v2

After independent verification of PR 145, a separate slice was opened from `0303a1514e2875a057ef1b20293a01b36a9c6b2b`. The lease-bound entry point requires exact persisted lease-v2 bytes, limits delegation to one spawn, hashes complete streams, and writes a durable terminal receipt. The production lease, image inspection, command, and Docker invocation remain absent.

### 2026-07-30 — `QW-LC4-E` final acknowledgement authoring

- independently verified PR #146 merged as `2957d8f6975c88e7bdb23243e3915c7f51d4ba47`;
- recorded `39` focused, `240` targeted, and `1287` full tests with `14` warnings;
- opened a static acknowledgement-authoring slice at the exact merge commit;
- the contract requires the exact operator phrase, operator identity, a UTC time after merge, and `invocation_count=1`;
- acknowledgement issuance, lease, outcome, authorization consumption, image inspection, command, Docker, and execution remain closed.


### 2026-07-30 — `QW-LC4-E` final acknowledgement issuance authoring

- independently verified PR #147 merged as `eb20c157584efff8e9aa0418385242c7d7b26eab`;
- recorded `50` focused, `251` targeted, and `1298` full tests with `14` warnings;
- opened a static issuance-authoring slice at the exact merge commit;
- the contract requires separate operator and issuer identities, ordered timestamps, one path, atomic no-overwrite persistence, mode `0600`, and `fsync`;
- writer implementation, acknowledgement, lease, outcome, authorization, image, command, Docker, and execution remain closed.


### 2026-07-30 — `QW-LC4-E` final acknowledgement issuance implementation

- independently verified PR #148 merged as `8343724c66b1d22f01846d9fc70f01738a09127a`;
- recorded `61` focused, `262` targeted, and `1309` full tests with `14` warnings;
- opened a separate implementation slice at the exact merge commit;
- implemented an atomic no-overwrite writer with mode `0600`, `fsync`, parent-chain, stale-temporary, and exact persisted-byte checks;
- no production callsite, acknowledgement, lease, outcome, image inspection, command, Docker, or execution exists.


### 2026-07-30 — `QW-LC4-E` final acknowledgement materialization authoring

- independently verified PR #149 merged as `31206012ef7cbd2b7b21a2017374c11123abd42c`;
- recorded `79` focused, `280` targeted, and `1327` full tests with `14` warnings;
- opened a static authoring slice at the exact merge commit;
- the contract binds operator, issuer, materializer, ordered UTC timestamps, exact writer, path, and SHA-256;
- no production callsite, acknowledgement, lease, outcome, image, command, Docker, or execution exists.


### 2026-07-30 — `QW-LC4-E` final acknowledgement materialization implementation

- independently verified PR #150 merge `6497cd904f9403622249c5a32f08ef6e8bb11532`;
- opened a separate bounded implementation slice at the exact merge commit;
- implemented one explicit materializer with one writer delegate and one postwrite reverification;
- no production callsite, acknowledgement, lease, outcome, image inspection, command, Docker, or execution exists.

### 2026-07-30 — `QW-LC4-E` final-acknowledgement materializer invocation authoring

- independently verified PR #151 merged as `7d5e5058af6a845cf4a6add2e7fe199894f48b24`;
- recorded `108` focused, `309` targeted, and `1356` full tests with `14` warnings and four successful CI checks;
- opened a pure authoring slice for the sole future materializer invocation;
- direct writer calls and automatic/blind retry are forbidden, while explicit recovery requires prior durable-state inspection;
- the materializer, writer, acknowledgement, lease, outcome, image, command, Docker, and execution remain absent.

### 2026-07-31 — `QW-LC4-E` final-acknowledgement materialization invocation implementation

- independently verified PR #152 merged as `febfba65d2f200fd2163928643eadd807a6b4d21`;
- recorded `124` focused, `325` targeted, and `1372` full tests with `14` warnings and four successful CI checks;
- implemented durable-state probing and at most one materializer delegation;
- a valid existing acknowledgement is success without another call, while an invalid target fails closed;
- no production callsite, acknowledgement, lease, outcome, Docker invocation, or execution exists.

### 2026-07-31 — `QW-LC4-E` materialization invocation operator-operation authoring

- independently verified PR #153 merged as `0ace9f1025100fa29ff0af7523fde17674c4852b`;
- recorded `144` focused, `345` targeted, and `1392` full tests with `14` warnings and four successful CI checks;
- opened a pure operation-authoring slice bound to the exact prospective invocation and a distinct action phrase;
- standalone pre-probing, adapter calls outside the future implementation, and direct materializer or writer calls are forbidden;
- no operation, acknowledgement, lease, outcome, or execution exists.


### 2026-07-31 — `QW-LC4-E` materialization invocation operator-operation implementation

- independently verified PR #154 merged as `5ee6d2346e558be19cfdf79e8a77b0568475bf4c`;
- recorded `162` focused, `363` targeted, and `1410` full tests with `14` warnings and four successful CI checks;
- implemented the library operation validator and sole exact adapter delegation;
- standalone pre-probing and direct materializer or writer calls are forbidden;
- no production callsite, operation, acknowledgement, lease, outcome, or execution exists.


## 2026-07-31 — QW-LC4-E: production operation-callsite contract

After post-merge verification of PR #155, a separate authoring slice was opened.
It freezes the exact future CLI path, explicit canonical operation file, single
library-operation call, and fail-closed result semantics. No production callsite
was added and the operation was not performed.

### 2026-08-01 — `QW-LC4-E`: operation production-callsite implementation

- PR #162 was reconciled after `main` advanced: first parent `dc8dc200515959858d43b68984dbd87f27f3446c`, merge `b27e252cf7c64e88d5d61bf7a23c70ffc5957959`, first-parent delta `18/1516/0`;
- the sole ADR-095 CLI was added with required `--project-root` and `--operation-json`;
- input must be exact canonical JSON and library-operation delegation is limited to one call;
- the result is verified and emitted canonically only on success, with no result file;
- behavioural effects are tested only in isolated temporary copies;
- no operation, acknowledgement, lease, outcome, Docker, or local compute exists in the primary worktree.



## 2026-08-01 — stage 4-E: production-callsite execution contract

After post-merge verification of PR #163, a separate authoring slice opened. It freezes a future separate authorization, exact canonical `operation.json`, one no-shell argv attempt, and repeated dynamic precondition verification. The production callsite was not executed.

### 2026-08-02 — `QW-LC4-E`: production-callsite execution authorization

- PR #164 merge `75936adac9ee100f9538f5af13a8ce312642ee0b` was independently verified;
- `240` focused, `441` targeted, and `1488` full tests with `14` warnings and four successful CI checks were recorded;
- a separate single-use authorization record and canonical prospective operation object are materialized;
- the authorization is bound to the operator, exact callsite, input SHA-256, and one future no-shell, no-retry attempt;
- until independent verification of the future merge, authorization is neither effective nor consumed, and no operation, acknowledgement, or local compute occurs.

### 2026-08-02 — `QW-LC4-E`: post-final-acknowledgement transition

- verified the five-file evidence package for production-callsite acknowledgement materialization;
- confirmed authorization consumption, completion of the single attempt, acknowledgement materialization, and retry prohibition;
- separately confirmed that no persistent lease v2, terminal host receipt, or runtime output exists;
- classified the package as completion of the acknowledgement line, not as the extension engineering report;
- `QW-5`, `C1/C2/C3/R`, test-data access, and publication remain closed;
- the next admissible slice is separate authoring of actual one-shot engineering-invocation admission through the lease-bound host invoker.

### 2026-08-03 — `QW-LC4-E`: final engineering-invocation admission-authoring scope freeze

- opened a separate scope-freeze branch after independent verification of PR #167 merge;
- froze exact identities for the post-acknowledgement transition, final evidence, persistent evidence chain v2, and lease-bound host invoker;
- designated `invoke_lease_bound_host_runtime` as the sole prospective future engineering-invocation entry point;
- historical authorizations and the direct operation are validation-only provenance inputs and cannot be reused;
- this slice creates no admission schema, verifier, admission tests, admission record, authorization, lease, host outcome, or runtime output;
- `QW-5` and the scientific campaign remain closed.

## 2026-08-03 — final engineering-invocation admission authoring

- added [ADR-101](../decisions/ADR-101-stage3b-qwake-lc4-e-final-engineering-invocation-admission-authoring_EN.md);
- materialized the pure schema, verifier, and canonical admission record;
- negative checks mutate temporary copies only;
- authorization, lease v2, durable host outcome, runtime output, and `QW-5` remain closed.

## 2026-08-03 — final engineering-invocation admission repository seal

- independently verified PR #169 merge `d2539eb440e758c1f29b935f8599561bec7126bc` with exact head `b81c11971f1e9b78e59dd39c4d182722a3001044`;
- confirmed the two-commit graph, merge graph, 17-file scope, final-head checks, `ruff`, four static guards, and 23 targeted tests;
- materialized a two-file repository receipt binding the admission record and its executable surfaces to exact `main` state;
- the receipt remains incomplete until its own merge and independent post-merge verification;
- the new authorization, operator phrase, invocation command, lease v2, durable host outcome, runtime output, and `QW-5` remain closed.

## 2026-08-03 — new one-shot authorization-authoring scope freeze

- independently verified PR #170 merge `a5b96edb1f82485561e0f52d6a98432d55ae8609` with exact head `6201997a71428bdb873d95d76c5b0882be532b2a`;
- confirmed the single-commit graph, merge graph, 14-file scope, final-head checks, `ruff`, four static checks, and 27 targeted tests;
- derived repository-seal completion from the immutable receipt, exact merge, and independent post-merge verification;
- froze exact inputs, future authoring surfaces, distinctness, and one-shot semantics for the new authorization;
- this slice creates no schema, verifier, tests, authorization record, operator phrase, command, lease, host outcome, or runtime output;
- `QW-5`, test-data access, and publication remain closed.

## 2026-08-03 — author the new one-shot final engineering-invocation authorization

- independently verified PR #171 merged as `61f190db2fbd4bf0ee8a58cac8b6841fbecc6cdd` with exact head `6093a18156036d8aa470c88844b0580cd3926c4e`;
- recorded the one-commit graph, merge graph, 13-file scope, final-head checks, `ruff`, four static checks, and 27 targeted tests;
- added [ADR-104](../decisions/ADR-104-stage3b-qwake-lc4-e-final-engineering-invocation-authorization-record-authoring_EN.md);
- materialized a pure schema, verifier, tests, and canonical record bound to operator `local-posix-account:dzmitry-prychyna`;
- the record is issued but remains ineffective before its own merge and independent post-merge verification;
- command, consumption, lease, host outcome, runtime output, and `QW-5` remain absent.

## 2026-08-03 — authorization consumption-attempt scope freeze

- independently verified PR #172 merged as `47bb24dc8fa95292be33428ba8bc7ee598c49b1e` with exact head `220ce31235a28d1583f90cde5acd8f87ca5c2752`;
- confirmed the single-commit graph, merge graph, 17-file scope, exact SHA-256 identities, eleven packages, `ruff`, four static checks, and 37 targeted tests;
- derived the effective, post-merge-verified, but unconsumed state of the one-shot authorization;
- added [ADR-105](../decisions/ADR-105-stage3b-qwake-lc4-e-final-engineering-invocation-authorization-consumption-attempt-scope-freeze_EN.md);
- froze future attempt-record surfaces, atomic consumption with attempt start and exclusive lease v2, and retry prohibition;
- the attempt record, consumption, command, lease, host outcome, runtime output, and `QW-5` remain absent.

## 2026-08-03 — authorization consumption-attempt record authoring

- independently verified PR #173 merged as `28b4627436244893195231f55f2d0d5fb2d1062e` with exact head `17af7d6f4473af846f2d293192082074cad99cf2`;
- confirmed the single-commit graph, merge graph, 13-file scope, exact SHA-256 identities, twelve packages, `ruff`, four static checks, and 37 targeted tests;
- added [ADR-106](../decisions/ADR-106-stage3b-qwake-lc4-e-final-engineering-invocation-authorization-consumption-attempt-record-authoring_EN.md);
- materialized a pure schema, verifier, tests, and distinct canonical attempt record;
- the record is prepared while authorization remains unconsumed and the attempt not started;
- command, lease v2, host outcome, runtime output, and `QW-5` remain absent.
